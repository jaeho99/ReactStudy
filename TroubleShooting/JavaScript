트러블 슈팅 JavaScript

1. 기호의 착각
강의 코드 예제를 따라하면서 코드 중 `count : ${count}`로 써야 했다.
하지만 백틱(``)을 쓰지 않고 작은 따옴표('')를 쓰게 되어 코드 실행이 안되었다.

문제 해결 과정
$ 선택자는 jQuery에서 사용하는 거 아닌가 라는 생각이 들어 기호는 생각하지 못 한채
왜 예제 코드에 jQuery를 불러오는 script 태그가 없는 건가? 라는 생각이 들었다.
그래서 script를 설치 하지 않고 적용할 수 있는 방법이 생긴건가 라는 생각에 열심히
구글 검색을 하던 도중 템플릿 리터럴에 대해 알게 되었고 기호가 잘못된 것을 알아채게 되었다.

템플릿 리터럴이 적용되지 않았던 것이다.

